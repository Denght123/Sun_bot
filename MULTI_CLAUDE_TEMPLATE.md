# MULTI_CLAUDE_TEMPLATE

## 使用说明
这是一份给你分发给多个 Claude Code 会话的任务模板。

用法：
1. 先让每个 Claude Code 阅读 `TempNews.md`
2. 再把下面某一段任务模板复制给对应 Claude Code
3. 每个 Claude Code 只负责自己的模块，不要越界修改别人的核心文件
4. 默认都必须遵守当前 MVP 边界，不新增未经确认的需求

---

## 通用前置说明（发给所有 Claude Code）
你正在参与一个“基金理财热点消息机器人”项目。

在开始工作前，请先阅读以下文档：
- `TempNews.md`
- `README.md`

然后根据你的负责方向，再阅读对应文档：
- 架构与后端：`TECH_DESIGN.md`
- 接口：`API_SPEC.md`
- 数据库：`DB_DESIGN.md`
- 任务拆解：`TASK_BREAKDOWN.md`

工作要求：
- 严格遵守当前 MVP 范围
- 不要新增产品需求
- 不要把个人微信账号密码、服务器密码、API Key 写入文件
- 尽量只修改你负责的模块文件
- 若发现需求不清晰，先记录问题，不要擅自扩展

项目当前最优先目标：
**先跑通“抓取 → 处理 → 摘要/模板生成 → 任务下发 → 微信发送”的最小主链路。**

---

## 模板 A：后端基础骨架 Claude
你的任务是负责 **后端基础骨架**。

请重点阅读：
- `TempNews.md`
- `TECH_DESIGN.md`
- `TASK_BREAKDOWN.md`

你的目标：
- 搭建 FastAPI 项目基础结构
- 增加配置管理（环境变量）
- 初始化日志组件
- 建立 PostgreSQL / Redis 基础连接
- 准备 Dockerfile / docker-compose 基础骨架

你的边界：
- 不负责抓取逻辑细节
- 不负责微信发送逻辑
- 不擅自定义新的业务模块

你完成后应交付：
- 清晰的后端目录结构
- 可启动的 FastAPI 服务骨架
- 基础配置和日志初始化能力

---

## 模板 B：数据库设计落地 Claude
你的任务是负责 **数据库与模型层落地**。

请重点阅读：
- `TempNews.md`
- `DB_DESIGN.md`
- `TASK_BREAKDOWN.md`

你的目标：
- 根据 `DB_DESIGN.md` 建立 MVP 所需核心表
- 设计 ORM Model / Migration
- 定义状态枚举
- 封装基础 repository / DAO

优先范围：
- `raw_items`
- `event_clusters`
- `daily_reports`
- `report_sections`
- `dispatch_tasks`
- `senders`
- `sender_heartbeats`

你的边界：
- 不负责抓取器实现
- 不负责发信端实现
- 不自行改产品字段定义，除非有明确理由并记录

你完成后应交付：
- 可迁移的数据库结构
- 基础模型层
- 明确的状态字段与索引策略

---

## 模板 C：抓取与规则引擎 Claude
你的任务是负责 **数据抓取与规则处理**。

请重点阅读：
- `TempNews.md`
- `TECH_DESIGN.md`
- `DB_DESIGN.md`
- `TASK_BREAKDOWN.md`

你的目标：
- 接入首批 1-2 个关键数据源
- 做原始数据标准化
- 实现基础去重
- 实现财经相关性过滤
- 实现基础分类与简单聚类

首批建议数据源：
- 财联社
- 微博热搜 或 百度热搜

你的边界：
- 不负责 LLM 摘要模块
- 不负责发信端
- 不把泛娱乐热点纳入正文

你完成后应交付：
- 可运行的数据抓取器
- 标准化后的原始数据结构
- 可供日报生成消费的候选事件列表

---

## 模板 D：日报生成与 LLM Claude
你的任务是负责 **日报生成与摘要模块**。

请重点阅读：
- `TempNews.md`
- `TECH_DESIGN.md`
- `PRD(基金理财热点消息机器人).md`
- `TASK_BREAKDOWN.md`

你的目标：
- 先实现规则模板版日报生成
- 再接入 LLM 摘要能力
- 输出完整精简版日报
- 生成各模块详情内容
- 控制字数与 message chunks 数量

必须遵守：
- 表达方式为“先事实，后影响”
- 中性克制
- 不输出投资建议
- 总长度约 1200-1800 字
- 总推送不超过 6 条消息

你的边界：
- 不负责抓取器实现
- 不负责微信发送
- 不重新定义模块结构

你完成后应交付：
- 模板日报生成器
- LLM 摘要逻辑
- 各模块内容拼装能力
- 降级方案（LLM 失败时可继续生成）

---

## 模板 E：调度与任务下发 Claude
你的任务是负责 **调度器与 dispatch task 流程**。

请重点阅读：
- `TempNews.md`
- `TECH_DESIGN.md`
- `API_SPEC.md`
- `DB_DESIGN.md`
- `TASK_BREAKDOWN.md`

你的目标：
- 接入 APScheduler
- 配置北京时间时区
- 实现日报生成调度
- 实现 dispatch task 创建
- 实现失败重试和状态流转
- 提供手动补跑入口

你的边界：
- 不负责具体微信自动化
- 不负责抓取器细节
- 不改动日报业务结构

你完成后应交付：
- 可运行的调度任务
- dispatch task 状态闭环
- 补跑与失败重试能力

---

## 模板 F：Windows 发信端 Claude
你的任务是负责 **Windows 发信端**。

请重点阅读：
- `TempNews.md`
- `TECH_DESIGN.md`
- `API_SPEC.md`
- `TASK_BREAKDOWN.md`

你的目标：
- 搭建 Sender Agent 基础骨架
- 实现心跳上报
- 实现待发送任务轮询
- 集成微信自动化发送
- 实现发送结果回传

必须遵守：
- 不要求用户提供微信账号密码
- 采用本机已登录微信会话
- 若微信掉线，设计为人工重新登录恢复

你的边界：
- 不负责云端抓取逻辑
- 不负责数据库核心建模

你完成后应交付：
- 可运行的 Windows 发信程序
- 与云端 API 的联通能力
- 发送结果状态回传能力

---

## 模板 G：测试与运维 Claude
你的任务是负责 **测试与运维支持**。

请重点阅读：
- `TempNews.md`
- `README.md`
- `TECH_DESIGN.md`
- `API_SPEC.md`
- `TASK_BREAKDOWN.md`

你的目标：
- 设计核心测试用例
- 覆盖主链路与异常链路
- 整理 ECS 部署检查项
- 梳理健康检查、日志与告警方案

优先关注：
- ECS 重启恢复
- 发信端离线
- 微信掉线
- LLM 失败降级
- 推送失败重试 3 次

你的边界：
- 不重新定义产品功能
- 不直接改动其他模块业务逻辑，除非是为测试可观测性补充必要能力

你完成后应交付：
- 测试清单
- 异常验证方案
- 部署检查清单
- 监控与告警建议

---

## 推荐分工方案（示例）
如果你准备同时开 5 个 Claude Code，可以这样分：

- **Claude 1**：后端基础骨架（模板 A）
- **Claude 2**：数据库与模型层（模板 B）
- **Claude 3**：抓取与规则引擎（模板 C）
- **Claude 4**：日报生成与 LLM（模板 D）
- **Claude 5**：调度与任务下发（模板 E）

如果你开 6 个或更多，再增加：
- **Claude 6**：Windows 发信端（模板 F）
- **Claude 7**：测试与运维支持（模板 G）

---

## 最后提醒
所有 Claude Code 都应围绕同一个核心目标工作：
**尽快打通最小主链路，而不是同时追求所有高级功能。**

优先级永远是：
1. 能跑起来
2. 能稳定生成日报
3. 能稳定发送微信
4. 再去优化内容质量和运维能力
